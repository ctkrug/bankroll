{
  "name": "bankroll",
  "version": "0.1.0",
  "private": true,
  "description": "Live Monte-Carlo bankroll simulator: edge, Kelly sizing, and risk of ruin, visualized.",
  "type": "module",
  "scripts": {
    "test": "node --test tests/",
    "lint": "node --check src/sim.js && node --check src/worker.js && node --check src/chart.js && node --check src/urlState.js && node --check src/main.js",
    "build": "node scripts/build-site.mjs",
    "start": "python3 -m http.server 8080"
  },
  "license": "MIT",
  "author": "Charlie Krug",
  "engines": {
    "node": ">=20"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/ctkrug/bankroll.git"
  },
  "bugs": {
    "url": "https://github.com/ctkrug/bankroll/issues"
  },
  "homepage": "https://github.com/ctkrug/bankroll#readme",
  "devDependencies": {
    "fast-check": "^4.8.0"
  }
}
